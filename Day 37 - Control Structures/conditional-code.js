// General syntax for conditional statements

const myName = 'Dio';

if (myName === 'Dio') {
    console.log('Hello');
};

const isLoggedIn = true;

if (!isLoggedIn) {
    console.log('User is NOT logged in!')
}

const enteredUserName = '';

if (enteredUserName) {
    console.log('Input is valid')
}